"""The four verification checks, one module each.

These were previously all inside verification/utils.py -- a name that told
readers there was nothing important inside, while in fact holding the whole
verification business: what counts as a cited source, what makes a plan
internally inconsistent, which safety flags block delivery, and where the
faithfulness pass threshold sits.
"""
