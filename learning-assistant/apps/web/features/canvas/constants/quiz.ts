/** Prefix of each question heading's id; its radio group points to it. */
export const QUESTION_LABEL_ID_PREFIX = "question-";

/** The chat message the New questions button sends to the assistant. */
export const NEW_QUESTIONS_MESSAGE = "Give me a new set of quiz questions.";
