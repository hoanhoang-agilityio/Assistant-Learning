/**
 * The canvas catalog: the basic A2UI catalog plus the learning components the
 * fixed stage templates use. The web app registers the React renderers under
 * this id (`apps/web/features/a2ui`).
 */
export const CANVAS_CATALOG_ID = "learning-assistant/canvas/v1";

/** The id every A2UI surface renders from. */
export const ROOT_COMPONENT_ID = "root";
