{
  "extends": "@repo/typescript-config/base.json",
  "compilerOptions": {
    // Source-only package: consumers import the .ts files directly, nothing is emitted.
    "noEmit": true
  },
  "include": ["src", "vitest.config.ts"],
  "exclude": ["node_modules"]
}
